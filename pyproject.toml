[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ghg-core"
version = "1.0.0"
description = "INVTY GHG calculation core - pure, deterministic, dependency-light"
requires-python = ">=3.11"
dependencies = []                       # the engine itself needs nothing

[project.optional-dependencies]
# pint is used ONLY to cross-check units in CI.
# sqlalchemy/alembic are needed by tests/test_database.py, which exercises the
# persistence layer. Without them pytest cannot even collect that module.
# Versions per docs/techstack-and-bugguard.md Part 1.
test = [
    "pytest>=8.0",
    "pint>=0.24",
    "sqlalchemy>=2.0,<2.1",
    "alembic>=1.13,<1.14",
]
# The Product Carbon service (service/). fastapi, pydantic and uvicorn are
# approved in docs/techstack-and-bugguard.md Part 1. anthropic is added for
# the AI estimator the Product Carbon page was explicitly asked to use.
service = [
    "fastapi>=0.115,<0.116",
    "pydantic>=2.9,<2.10",
    "uvicorn[standard]>=0.32,<0.33",
    "anthropic>=1.6,<2",
]

# Explicit, so setuptools never tries to treat data/ or tests/ as packages.
[tool.setuptools]
packages = ["ghg_core"]

[tool.setuptools.package-data]
ghg_core = ["py.typed"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = [".", "tests"]
